namespace RandomTaskTrack.Data.Models.Enums;

/// <summary>
/// Matches ck_plant_plants_kind. A packet becomes a Plant when it comes up —
/// one column change, keeping the photos and the tasks it already has.
/// </summary>
public enum PlantKind
{
    Plant = 1,
    SeedPacket = 2
}
