# `_draft`

Scratch space. Runs on every migration and is **not** version-tracked — use it
while iterating on a script locally, then move the final version into a `vX.XX`
directory before committing.
